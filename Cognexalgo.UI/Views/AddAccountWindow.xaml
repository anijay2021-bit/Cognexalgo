<Window x:Class="Cognexalgo.UI.Views.AddAccountWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Cognexalgo.UI.ViewModels"
        mc:Ignorable="d"
        Title="Add New Account" Height="500" Width="400"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}"
        Foreground="{StaticResource TextDarkBrush}"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True">

    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Background="{StaticResource CardBrush}">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="ADD TRADING ACCOUNT" Style="{StaticResource HeaderTextStyle}" HorizontalAlignment="Center"/>
                <TextBlock Text="Configure your broker credentials below" Style="{StaticResource LabelTextStyle}" HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- Content -->
            <StackPanel Grid.Row="1">
                <!-- Account Name -->
                <TextBlock Text="Account Nickname" Style="{StaticResource LabelTextStyle}"/>
                <TextBox Text="{Binding AccountName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBoxStyle}" Margin="0,0,0,15" Tag="e.g. My Savings Fund"/>

                <!-- Broker Selection -->
                <TextBlock Text="Broker" Style="{StaticResource LabelTextStyle}"/>
                <ComboBox SelectedItem="{Binding Broker}" Style="{StaticResource DarkComboBoxStyle}" Margin="0,0,0,15">
                    <ComboBoxItem Content="Angel One"/>
                    <ComboBoxItem Content="Zerodha"/>
                    <ComboBoxItem Content="Alice Blue"/>
                </ComboBox>

                <!-- Client ID -->
                <TextBlock Text="Client Code / ID" Style="{StaticResource LabelTextStyle}"/>
                <TextBox Text="{Binding ClientId, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBoxStyle}" Margin="0,0,0,15"/>

                <!-- API Key -->
                <TextBlock Text="API Key" Style="{StaticResource LabelTextStyle}"/>
                <TextBox Text="{Binding ApiKey, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBoxStyle}" Margin="0,0,0,15"/>

                <!-- TOTP Key -->
                <TextBlock Text="TOTP Enabling Key" Style="{StaticResource LabelTextStyle}"/>
                <TextBox Text="{Binding TotpKey, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBoxStyle}" Margin="0,0,0,15"/>
            </StackPanel>

            <!-- Footer -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="CANCEL" Command="{Binding CancelCommand}" Click="Cancel_Click" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,0" Width="100"/>
                <Button Content="SAVE ACCOUNT" Command="{Binding SaveCommand}" Style="{StaticResource ActionPushButtonStyle}" Width="150"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
